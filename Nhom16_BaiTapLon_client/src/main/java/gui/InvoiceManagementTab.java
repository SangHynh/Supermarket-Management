package gui;

import javax.swing.*;
import java.awt.*;

public class InvoiceManagementTab extends JPanel {
    public InvoiceManagementTab() {
        setBackground(Color.YELLOW);
        JLabel label = new JLabel("Invoice Management");
        add(label);
    }
}
